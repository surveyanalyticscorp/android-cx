package com.questionpro.cxlib;

enum InterceptCondition {
    AND,
    OR
}


enum InterceptType {
    PROMPT,
    EMBED,
    SURVEY_URL
}

enum InterceptRuleType {
    TIME_SPENT,
    VIEW_COUNT,
    DAY,
    DATE
}